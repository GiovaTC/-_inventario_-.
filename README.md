# -_inventario_-. :.  
# 🧮 Gestor de Inventario — JavaFX + Oracle 19c .

<img width="1024" height="1024" alt="image" src="https://github.com/user-attachments/assets/d24de3e2-a68a-4c78-b540-05ef210ec2bc" />  
  
Proyecto en **Java** que gestiona un inventario de productos, con interfaz gráfica **JavaFX** y persistencia de datos en **Oracle Database 19c**.

---

## 📋 Características :

✅ CRUD completo (Crear, Leer, Actualizar, Eliminar)  
✅ Conexión a Oracle mediante JDBC  
✅ Interfaz moderna en JavaFX (FXML opcional)  
✅ Validaciones básicas de campos  
✅ Clase DAO para separar la lógica de base de datos  
✅ Preparado para ejecutarse en IntelliJ IDEA  

---

## 🏗️ Estructura del proyecto :

- **Inventario/**
  - **src/**
    - **dao/**
      - `ProductoDAO.java`
    - **db/**
      - `ConexionOracle.java`
    - **model/**
      - `Producto.java`
    - **ui/**
      - `InventarioApp.java`
  - **resources/**
    - `inventario.fxml`
  - `pom.xml` *(si usas Maven)*
  - `README.md`

---

## 🧱 SQL — Creación de tabla en Oracle 19c :

```sql
CREATE TABLE PRODUCTOS (
    ID_PRODUCTO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE VARCHAR2(100) NOT NULL,
    CATEGORIA VARCHAR2(50),
    CANTIDAD NUMBER DEFAULT 0,
    PRECIO NUMBER(10,2)
);

INSERT INTO PRODUCTOS (NOMBRE, CATEGORIA, CANTIDAD, PRECIO) VALUES ('Laptop Lenovo', 'Electrónica', 15, 3200);
INSERT INTO PRODUCTOS (NOMBRE, CATEGORIA, CANTIDAD, PRECIO) VALUES ('Mouse Logitech', 'Periféricos', 50, 120);
INSERT INTO PRODUCTOS (NOMBRE, CATEGORIA, CANTIDAD, PRECIO) VALUES ('Teclado Redragon', 'Periféricos', 30, 200);
COMMIT;

⚙️ Clase de conexión — ConexionOracle.java

package db;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

public class ConexionOracle {
    private static final String URL = "jdbc:oracle:thin:@localhost:1521:xe";
    private static final String USER = "system";
    private static final String PASSWORD = "oracle";

    public static Connection getConnection() throws SQLException {
        try {
            Class.forName("oracle.jdbc.driver.OracleDriver");
            return DriverManager.getConnection(URL, USER, PASSWORD);
        } catch (ClassNotFoundException e) {
            throw new SQLException("Error: No se encontró el driver de Oracle", e);
        }
    }
}

🧩 Clase modelo — Producto.java

package model;

public class Producto {
    private int idProducto;
    private String nombre;
    private String categoria;
    private int cantidad;
    private double precio;

    public Producto(int idProducto, String nombre, String categoria, int cantidad, double precio) {
        this.idProducto = idProducto;
        this.nombre = nombre;
        this.categoria = categoria;
        this.cantidad = cantidad;
        this.precio = precio;
    }

    public Producto(String nombre, String categoria, int cantidad, double precio) {
        this(0, nombre, categoria, cantidad, precio);
    }

    public int getIdProducto() { return idProducto; }
    public String getNombre() { return nombre; }
    public String getCategoria() { return categoria; }
    public int getCantidad() { return cantidad; }
    public double getPrecio() { return precio; }

    public void setNombre(String nombre) { this.nombre = nombre; }
    public void setCategoria(String categoria) { this.categoria = categoria; }
    public void setCantidad(int cantidad) { this.cantidad = cantidad; }
    public void setPrecio(double precio) { this.precio = precio; }
}

💾 Clase DAO — ProductoDAO.java

package dao;

import db.ConexionOracle;
import model.Producto;

import java.sql.*;
import java.util.ArrayList;
import java.util.List;

public class ProductoDAO {

    public List<Producto> listar() {
        List<Producto> lista = new ArrayList<>();
        String sql = "SELECT * FROM PRODUCTOS ORDER BY ID_PRODUCTO";
        try (Connection conn = ConexionOracle.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql);
             ResultSet rs = ps.executeQuery()) {
            while (rs.next()) {
                lista.add(new Producto(
                        rs.getInt("ID_PRODUCTO"),
                        rs.getString("NOMBRE"),
                        rs.getString("CATEGORIA"),
                        rs.getInt("CANTIDAD"),
                        rs.getDouble("PRECIO")
                ));
            }
        } catch (SQLException e) {
            e.printStackTrace();
        }
        return lista;
    }

    public boolean insertar(Producto p) {
        String sql = "INSERT INTO PRODUCTOS (NOMBRE, CATEGORIA, CANTIDAD, PRECIO) VALUES (?, ?, ?, ?)";
        try (Connection conn = ConexionOracle.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setString(1, p.getNombre());
            ps.setString(2, p.getCategoria());
            ps.setInt(3, p.getCantidad());
            ps.setDouble(4, p.getPrecio());
            ps.executeUpdate();
            return true;
        } catch (SQLException e) {
            e.printStackTrace();
        }
        return false;
    }

    public boolean actualizar(Producto p) {
        String sql = "UPDATE PRODUCTOS SET NOMBRE=?, CATEGORIA=?, CANTIDAD=?, PRECIO=? WHERE ID_PRODUCTO=?";
        try (Connection conn = ConexionOracle.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setString(1, p.getNombre());
            ps.setString(2, p.getCategoria());
            ps.setInt(3, p.getCantidad());
            ps.setDouble(4, p.getPrecio());
            ps.setInt(5, p.getIdProducto());
            ps.executeUpdate();
            return true;
        } catch (SQLException e) {
            e.printStackTrace();
        }
        return false;
    }

    public boolean eliminar(int id) {
        String sql = "DELETE FROM PRODUCTOS WHERE ID_PRODUCTO=?";
        try (Connection conn = ConexionOracle.getConnection();
             PreparedStatement ps = conn.prepareStatement(sql)) {
            ps.setInt(1, id);
            ps.executeUpdate();
            return true;
        } catch (SQLException e) {
            e.printStackTrace();
        }
        return false;
    }
}

🪟 Interfaz JavaFX — InventarioApp.java

package ui;

import dao.ProductoDAO;
import javafx.application.Application;
import javafx.collections.FXCollections;
import javafx.collections.ObservableList;
import javafx.geometry.Insets;
import javafx.scene.Scene;
import javafx.scene.control.*;
import javafx.scene.layout.*;
import javafx.stage.Stage;
import model.Producto;

public class InventarioApp extends Application {
    private TableView<Producto> tabla;
    private TextField txtNombre, txtCategoria, txtCantidad, txtPrecio;
    private final ProductoDAO dao = new ProductoDAO();

    @Override
    public void start(Stage stage) {
        Label titulo = new Label("🧮 Gestor de Inventario — Oracle 19c");
        titulo.setStyle("-fx-font-size: 18px; -fx-font-weight: bold;");

        tabla = new TableView<>();
        tabla.getColumns().addAll(
                col("ID", "idProducto", 60),
                col("Nombre", "nombre", 150),
                col("Categoría", "categoria", 100),
                col("Cantidad", "cantidad", 100),
                col("Precio", "precio", 100)
        );
        actualizarTabla();

        txtNombre = new TextField(); txtNombre.setPromptText("Nombre");
        txtCategoria = new TextField(); txtCategoria.setPromptText("Categoría");
        txtCantidad = new TextField(); txtCantidad.setPromptText("Cantidad");
        txtPrecio = new TextField(); txtPrecio.setPromptText("Precio");

        Button btnAgregar = new Button("Agregar");
        btnAgregar.setOnAction(e -> agregar());

        Button btnEliminar = new Button("Eliminar");
        btnEliminar.setOnAction(e -> eliminar());

        HBox inputs = new HBox(10, txtNombre, txtCategoria, txtCantidad, txtPrecio, btnAgregar, btnEliminar);
        inputs.setPadding(new Insets(10));

        VBox root = new VBox(10, titulo, tabla, inputs);
        root.setPadding(new Insets(15));

        stage.setTitle("Gestor de Inventario");
        stage.setScene(new Scene(root, 650, 400));
        stage.show();
    }

    private void agregar() {
        try {
            Producto p = new Producto(
                    txtNombre.getText(),
                    txtCategoria.getText(),
                    Integer.parseInt(txtCantidad.getText()),
                    Double.parseDouble(txtPrecio.getText())
            );
            if (dao.insertar(p)) {
                limpiarCampos();
                actualizarTabla();
            }
        } catch (Exception e) {
            alerta("Error", "Datos inválidos: " + e.getMessage());
        }
    }

    private void eliminar() {
        Producto p = tabla.getSelectionModel().getSelectedItem();
        if (p != null && dao.eliminar(p.getIdProducto())) {
            actualizarTabla();
        } else {
            alerta("Atención", "Selecciona un producto primero.");
        }
    }

    private void actualizarTabla() {
        ObservableList<Producto> datos = FXCollections.observableArrayList(dao.listar());
        tabla.setItems(datos);
    }

    private void limpiarCampos() {
        txtNombre.clear(); txtCategoria.clear(); txtCantidad.clear(); txtPrecio.clear();
    }

    private TableColumn<Producto, ?> col(String titulo, String campo, int ancho) {
        TableColumn<Producto, Object> c = new TableColumn<>(titulo);
        c.setCellValueFactory(new javafx.scene.control.cell.PropertyValueFactory<>(campo));
        c.setPrefWidth(ancho);
        return c;
    }

    private void alerta(String titulo, String msg) {
        new Alert(Alert.AlertType.INFORMATION, msg, ButtonType.OK).showAndWait();
    }

    public static void main(String[] args) {
        launch();
    }
}

⚙️ Requisitos
Requisito	Versión recomendada
Java	17 o superior
Oracle Database	19c
IntelliJ IDEA	2023+
Driver JDBC Oracle	ojdbc8.jar

🧩 Ejecución
Crea la tabla PRODUCTOS en tu base de datos Oracle.

Coloca ojdbc8.jar en tu proyecto (o agrégalo en el pom.xml si usas Maven).

Ejecuta la clase InventarioApp desde IntelliJ IDEA.

Añade, consulta y elimina productos fácilmente.

📦 Autor: Giovanny Alejandro Tapiero Cataño
💡 Proyecto educativo — Gestión de Inventario con JavaFX y Oracle 19c .
